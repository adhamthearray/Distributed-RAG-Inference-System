events {}

http {
    upstream backend_servers {
        server node1:5000;
        server node2:5000;
        server node3:5000;
    }

    server {
        listen 80;

        location /predict {
            proxy_pass http://backend_servers;
        }
    }
}